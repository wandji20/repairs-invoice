<nav class="container-fluid px-1 py-2 d-flex justify-content-between align-items-center">
  <div class="col-4 d-flex justify-content-center align-items-center">
    <span>F/Autos</span>
    <span>icon</span>
  </div>

  
  <% if user_signed_in? && current_user.admin? %>
    <div class="col-4 d-flex justify-content-center align-items-center">
      <span class="bg-primary px-2 py-1 border">
        <%= link_to 'New Invoice', 
                  new_invoice_path, 
                  class: "text-white text-decoration-none " 
        %>
      </span>
    </div>
  <% end %>
  

  <div class="col-4 d-flex justify-content-center align-items-center">
    <ul class="list-unstyled m-0 d-flex justify-content-between">
      <% if user_signed_in? %>
        <li>
          <%= current_user.username %>
        </li>
        <li>
          <%= link_to 'logout', logout_path, class: "text-dark text-decoration-none " %>
        </li>
      <% else %>
        <li>
          <%= link_to 'login', new_session_path, class: "text-dark text-decoration-none "  %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>